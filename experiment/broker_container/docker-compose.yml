version: "3.0"
services:
  broker:
    image: my_mosq
    volumes:
      - ./broker_data:/broker/data
      - ./broker_log:/broker/log
  # container_name: sub_b1
    networks:
            - my-net
    tty: true

networks:
        default:
                external:
                        name: my-net
